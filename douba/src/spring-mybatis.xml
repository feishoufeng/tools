<?xml version="1.0" encoding="UTF-8"?>
<beans xmlns="http://www.springframework.org/schema/beans"
	xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance" xmlns:p="http://www.springframework.org/schema/p"
	xmlns:context="http://www.springframework.org/schema/context"
	xmlns:mvc="http://www.springframework.org/schema/mvc"
	xsi:schemaLocation="http://www.springframework.org/schema/beans  
                        http://www.springframework.org/schema/beans/spring-beans-4.3.xsd  
                        http://www.springframework.org/schema/context  
                        http://www.springframework.org/schema/context/spring-context-4.3.xsd  
                        http://www.springframework.org/schema/mvc  
                        http://www.springframework.org/schema/mvc/spring-mvc-4.3.xsd">
	<!-- 自动扫描 -->
	<context:component-scan base-package="com.dopakoala.douba" />
	<!-- 引入配置文件 -->
	<bean id="propertyConfigurer"
		class="org.springframework.beans.factory.config.PropertyPlaceholderConfigurer">
		<property name="locations" value="classpath:*.properties" />
	</bean>

	<bean id="dataSource" class="org.apache.commons.dbcp2.BasicDataSource" destroy-method="close">
		<!-- 用户名 -->
		<property name="username"
			value="#{'${server}'=='test_'?'${test_username}':'${server}'=='local_'?'${local_username}':'${release_username}'}" />
		<!-- 密码 -->
		<property name="password"
			value="#{'${server}'=='test_'?'${test_password}':'${server}'=='local_'?'${local_password}':'${release_password}'}" />
		<!-- 驱动 -->
		<property name="driverClassName"
			value="#{'${server}'=='test_'?'${test_driverClassName}':'${server}'=='local_'?'${local_driverClassName}':'${release_driverClassName}'}" />
		<!-- 连接地址 -->
		<property name="url"
			value="#{'${server}'=='test_'?'${test_url}':'${server}'=='local_'?'${local_url}':'${release_url}'}" />
		<!-- 连接参数 -->
		<property name="connectionProperties"
			value="#{'${server}'=='test_'?'${test_connectionProperties}':'${server}'=='local_'?'${local_connectionProperties}':'${release_connectionProperties}'}"></property>
		<!-- 初始化连接大小 -->
		<property name="initialSize"
			value="#{'${server}'=='test_'?'${test_initialSize}':'${server}'=='local_'?'${local_initialSize}':'${release_initialSize}'}"></property>
		<!-- 连接池最大数量 -->
		<property name="maxTotal"
			value="#{'${server}'=='test_'?'${test_maxTotal}':'${server}'=='local_'?'${local_maxTotal}':'${release_maxTotal}'}"></property>
		<!-- 连接池最大空闲 -->
		<property name="maxIdle"
			value="#{'${server}'=='test_'?'${test_maxIdle}':'${server}'=='local_'?'${local_maxIdle}':'${release_maxIdle}'}"></property>
		<!-- 连接池最小空闲 -->
		<property name="minIdle"
			value="#{'${server}'=='test_'?'${test_minIdle}':'${server}'=='local_'?'${local_minIdle}':'${release_minIdle}'}"></property>
		<!-- 获取连接最大等待时间 -->
		<property name="maxWaitMillis"
			value="#{'${server}'=='test_'?'${test_maxWaitMillis}':'${server}'=='local_'?'${local_maxWaitMillis}':'${release_maxWaitMillis}'}"></property>
	</bean>

	<!-- spring和MyBatis完美整合，不需要mybatis的配置映射文件 -->
	<bean id="sqlSessionFactory" class="org.mybatis.spring.SqlSessionFactoryBean">
		<property name="dataSource" ref="dataSource" />
		<!-- 自动扫描mapping.xml文件 -->
		<property name="mapperLocations" value="classpath:com/dopakoala/douba/mapping/*.xml"></property>
	</bean>

	<!-- DAO接口所在包名，Spring会自动查找其下的类 -->
	<bean class="org.mybatis.spring.mapper.MapperScannerConfigurer">
		<property name="basePackage" value="com.dopakoala.douba.dao" />
		<property name="sqlSessionFactoryBeanName" value="sqlSessionFactory"></property>
	</bean>

	<!-- (事务管理)transaction manager, use JtaTransactionManager for global tx -->
	<bean id="transactionManager"
		class="org.springframework.jdbc.datasource.DataSourceTransactionManager">
		<property name="dataSource" ref="dataSource" />
	</bean>
</beans>
